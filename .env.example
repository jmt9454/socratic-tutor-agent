# Copy to .env on the droplet (never commit .env)
OPENAI_API_KEY=sk-...

# Optional shared secret between OpenWebUI pipe and this API.
# If set, the pipe's API_KEY valve must match. If empty/absent, auth is
# disabled and access control is network isolation only.
# Generate one:  openssl rand -hex 32
TUTOR_API_KEY=
